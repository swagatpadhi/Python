l1 = [2, 4, 46, 1, 24, 21, 99, 3, 5]
# l1.sort()
# l1.reverse()
# l1.append("Swagat Bhai")
print(l1.pop()) #we are trying to print the return value of the method 
#pop() as well 
print(l1.pop(2))
print(l1)

#Unlike Strings, Lists are mutable.

# pop(): Use when you need to remove and access an 
# element by its index or when removing the last item.
# remove(): Use when you need to remove a specific 
# value from the list, without needing to know its index.